% Preamble: Packages and custom commands

% Encoding and fonts
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Graphics and figures
\usepackage{graphicx}
\usepackage{subcaption}

% Mathematics
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{listings}

\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  breakatwhitespace=true,
  language=SQL,
  showstringspaces=false,
}

% Hyperlinks
\usepackage[hidelinks]{hyperref}
% --- SOFT DELETE: numeric natbib options (Batch 5) ---
%\usepackage[numbers,sort&compress]{natbib}
% --- END SOFT DELETE ---
\usepackage[authoryear,round]{natbib}
\usepackage{enumitem} % For compact lists
\setlist[enumerate]{leftmargin=*, topsep=0pt, partopsep=0pt, parsep=0pt, itemsep=0pt}
\setlist[itemize]{leftmargin=*, topsep=0pt, partopsep=0pt, parsep=0pt, itemsep=0pt}

% --- SOFT DELETE: numeric thebibliography override (Batch 5) ---
% The old override used numeric labels (\@biblabel{\@arabic\c@enumiv}).
% Replaced by an author-date compatible version below.
% --- END SOFT DELETE ---
% Bibliography override: author-date compatible, with class heading and font
\makeatletter
\renewenvironment{thebibliography}[1]{%
    \section*{References and Notes}%
    \NINE
    \list{}%
    {\leftmargin 1.5em
     \itemindent -1.5em
     \itemsep 5pt}%
    \def\newblock{\hskip .11em plus .33em minus .07em}%
    \sloppy\clubpenalty4000\widowpenalty4000
    \sfcode`\.=1000\relax
}{%
    \endlist
}
\makeatother

% Custom commands
\newcommand{\note}[1]{\textcolor{red}{#1}}
